{
    "private": true,
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "install:all": "npm install",
        "dev:frontend": "cd frontend && npm run ionic-start",
        "dev:backend": "cd backend && npm run dev",
        "dev": "concurrently \"npm:dev:frontend\" \"npm:dev:backend\""
    },
    "devDependencies": {
        "@types/node": "^24.10.0",
        "concurrently": "^7.0.0",
        "tsx": "^4.20.6",
        "typescript": "^5.9.3"
    }
}